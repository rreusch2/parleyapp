[build]
builder = "nixpacks"

[deploy]  
startCommand = "npm start --prefix backend"
healthcheckPath = "/api/health"

[nixpacks]
aptPkgs = ["nodejs", "npm"]
installPhase = "npm install --prefix backend"
buildPhase = "npm run build:babel --prefix backend"
