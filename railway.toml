[build]
builder = "nixpacks"

[deploy]  
startCommand = "cd backend && npm run build && npm start"
healthcheckPath = "/api/health"

[nixpacks]
provider = "node"
installPhase = "cd backend && npm install"
buildPhase = "cd backend && npm run build"
startCommand = "cd backend && npm start"
