# Agent Service Environment Variables
# Copy to .env and fill in your values

# Supabase (for storage uploads and database access)
SUPABASE_URL=https://iriaegoipkjtktitpary.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Web App Base URL (where to POST events and messages back)
WEB_API_BASE_URL=http://localhost:3000

# Storage bucket name (optional, defaults to professor-lock-artifacts)
PROFESSOR_LOCK_BUCKET=professor-lock-artifacts

# Port (optional, defaults to 8000)
PORT=8000
